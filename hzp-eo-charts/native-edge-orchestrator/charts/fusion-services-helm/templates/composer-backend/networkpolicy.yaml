{{- if and (.Values.composer).enabled (.Values.composer_backend).enableNetworkPolicy -}}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ template "fusion-services.name" . }}-composer-backend-ingress-policy
  annotations:
    {{- include "fusion-services.merge" ( list .Values.commonAnnotations "" ) | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app: composer-backend
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: nginx
      ports:
        - protocol: TCP
          port: {{ .Values.composer_backend.port }}
{{- end }}
