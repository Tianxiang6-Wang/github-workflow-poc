{{- if .Values.agent.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fusion-services.name" . }}-agent-config
  annotations:
    {{- include "fusion-services.merge" ( list .Values.commonAnnotations "" ) | nindent 4 }}
data:
  config.yaml: |
    {{ .Values.agent.config | toYaml | nindent 4 }}
{{- end }}
