apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fusion-services.name" . }}-nginx-templates
  annotations:
    {{- include "fusion-services.merge" ( list .Values.commonAnnotations "" ) | nindent 4 }}
data:
{{ (tpl (.Files.Glob "nginx-configs/templates/*").AsConfig .) | indent 2 }}
