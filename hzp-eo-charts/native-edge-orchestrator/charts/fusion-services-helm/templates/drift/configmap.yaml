{{- if .Values.drift.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fusion-services.name" . }}-drift-config
  annotations:
    {{- include "fusion-services.merge" ( list .Values.commonAnnotations "" ) | nindent 4 }}
data:
  config.yaml: |
    {{ .Values.drift.config | toYaml | nindent 4 }}
{{- end }}
