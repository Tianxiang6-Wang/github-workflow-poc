{{ if .Values.victoriametrics.deploy }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart.fullname" . }}-vminsert
  labels:
    app: vminsert
    chart: {{ include "chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
  - port: 8480
  selector:
    app: vminsert
    release: {{ include "selector.label.release.name" . }}
{{- end }}
