---
apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ .Values.svc.name }}-headless
spec:
  clusterIP: None
  selector:
    hzp-app: {{ .Values.svc.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8012
