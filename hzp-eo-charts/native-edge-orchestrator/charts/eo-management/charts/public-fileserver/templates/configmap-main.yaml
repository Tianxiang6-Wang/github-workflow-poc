apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fileserver.name" . }}-main-config
  annotations:
    {{- include "common.annotations" ( list $ .Values.deploymentAnnotations ) | nindent 4 }}
data:
  nginx.conf: |
{{ .Files.Get "files/nginx.conf" | indent 4 }}
