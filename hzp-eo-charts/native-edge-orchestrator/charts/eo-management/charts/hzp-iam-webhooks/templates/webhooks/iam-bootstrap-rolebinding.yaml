apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Values.bootstrap.name }}-binding
subjects:
  - kind: ServiceAccount
    name: {{ .Values.bootstrap.name }}
roleRef:
  kind: Role
  name: {{ .Values.bootstrap.name }}-role
  apiGroup: rbac.authorization.k8s.io
