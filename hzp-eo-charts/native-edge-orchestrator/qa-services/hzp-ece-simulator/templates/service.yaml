---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.svc.name }}
  labels:
    hzp-app: {{ .Values.svc.name }}
spec:
  type: {{ .Values.svc.service.type }}
  ports:
    - port: {{ .Values.svc.service.port }}
      targetPort: {{ .Values.svc.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    hzp-app: {{ .Values.svc.name }}
